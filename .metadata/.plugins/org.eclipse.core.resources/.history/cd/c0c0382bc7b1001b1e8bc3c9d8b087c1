package com.servebeer.quake;

import org.bukkit.event.EventHandler;
import org.bukkit.event.player.PlayerJoinEvent;
import org.bukkit.event.player.PlayerRespawnEvent;
import org.bukkit.event.EventHandler;
import org.bukkit.event.Listener;
import org.bukkit.entity.Player;
import org.bukkit.Statistic;
import org.bukkit.ChatColor;
import org.bukkit.scheduler.BukkitRunnable;
import org.bukkit.scheduler.BukkitTask;

public class DeathStatistic extends BukkitRunnable
{

	@Override
	public void run() {
		
	}
	
}

class DeathStatisticJoin implements Listener {
	
	public void onPlayerJoin(PlayerJoinEvent event)
	{
		Player player = event.getPlayer();
		int deaths;
		deaths = player.getStatistic(Statistic.DEATHS);
		
		player.setPlayerListName(ChatColor.GOLD + " " + deaths + " " + ChatColor.WHITE + player.getName());
		
		player.setPlayerListHeader("Whomst'd've Minecraft Server");
	}
}

class DeathStatisticUpdate implements Listener {
	
	@EventHandler
	public void onPlayerJoin(PlayerRespawnEvent event)
	{
		Player player = event.getPlayer();
		int deaths;
		deaths = player.getStatistic(Statistic.DEATHS);
		
		player.setPlayerListName(ChatColor.GOLD + " " + deaths + " " + ChatColor.WHITE + player.getName());
	}
}
